{
  "name": "meting-enhanced-vercel",
  "version": "0.2.0",
  "private": true,
  "description": "Meting-API compatible Vercel adapter with api-enhanced backend and allowlist admin panel.",
  "main": "api/index.js",
  "scripts": {
    "dev": "vercel dev",
    "start": "node api/index.js",
    "lint:syntax": "node -c api/index.js && node -c lib/meting.js && node -c lib/ncm.js && node -c lib/http.js && node -c lib/allowlist.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@neteasecloudmusicapienhanced/api": "^4.35.1",
    "@upstash/redis": "^1.34.4"
  },
  "devDependencies": {
    "vercel": "^34.3.0"
  }
}
